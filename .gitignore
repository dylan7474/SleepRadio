*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing (local only)
keystore.properties
*.jks
*.keystore

# TTS voice packs — never committed (see NOTICE). A gitignored
# app/src/main/assets/tts/personal/ is auto-imported on the owner's own builds;
# the stock voice is a runtime download / file-picker import.
/app/src/main/assets/tts/

# Stock-voice build output (voice/eSpeak data is never committed)
/tools/build/
